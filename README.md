# Cinemabot

## env
* `BOT_TOKEN` – telegram bot token
* `GOOGLE_CREDENTIALS` – json string to access google sheets
* `SHEET_ID` – sheet with movies

## Usage
### Restart

```bash
ssh opc@92.5.112.79 "sudo systemctl restart cinemabot"
```